<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TagKosha" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TagKosha" parent="Base.Theme.TagKosha" >
        <item name="chipStyle">@style/Widget.App.Chip.TextLike</item>
    </style>
    <style name="Widget.App.Chip.TextLike" parent="Widget.MaterialComponents.Chip.Action">
        <item name="chipBackgroundColor">@android:color/transparent</item>
        <item name="chipMinHeight">0dp</item>
        <item name="chipStartPadding">0dp</item>
        <item name="chipEndPadding">8dp</item>
        <item name="chipStrokeWidth">0dp</item>
        <item name="ensureMinTouchTargetSize">false</item>
        <item name="chipMinTouchTargetSize">0dp</item>
        <item name="android:textColor">?attr/colorPrimary</item>
        <item name="rippleColor">?attr/colorControlHighlight</item>
    </style>
</resources>